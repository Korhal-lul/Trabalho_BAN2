# Trabalho_BAN2

Resumo install

cria e ativa o venv (nomeia ele .venv para ir pro gitignore)
pip install -r .\requirements.txt

cria um .env e coloca um 
DATABASE_URL= seu db
SECRET_KEY= chave random

liga e cria o db

python .\app.py